#!/usr/bin/env node

const fs = require('fs');
const path = require('path');
const isDev = false;//fs.existsSync(path.join(__dirname, '../tsconfig.json'));

if (isDev) {
  require('ts-node/register');
}

require(`../${isDev ? 'src' : 'lib'}`)
  .run()
  .catch(require('@oclif/errors/handle'));
