import * as t from '@/actions/types';

const initialState = '';

export default (state = initialState, action = {}) => {
<%=indent()%>const { type, payload } = action;

<%=indent()%>if (type === t.MY_ACTION) {
<%=indent(2)%>return 'newState';
<%=indent()%>}

<%=indent()%>return state;
};

export const <%=SELECTOR_NAME%> = state => state.<%=STATE_PATH%>;
